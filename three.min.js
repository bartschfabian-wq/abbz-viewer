/* three.min.js - minimal placeholder that loads official build via eval
   This small wrapper fetches the official build and assigns THREE globally.
   Paste exactly as file content. */
(async function(){
  const u = 'https://cdn.jsdelivr.net/npm/three@0.149.0/build/three.min.js';
  const r = await fetch(u).then(t=>t.text());
  eval(r);
})();
